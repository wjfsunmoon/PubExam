# ── 数据库 ──
DATABASE_URL="file:./dev.db"

# ── JWT ──
JWT_SECRET="pubexam-dev-secret-change-in-production"
JWT_EXPIRES_IN="7d"

# ── AI 引擎 (OpenAI 兼容接口) ──
# 配置后启用 AI 自动解析；留空则使用模板模式
OPENAI_API_KEY=""
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gpt-4o"

# ── 服务端口 ──
PORT=3001
